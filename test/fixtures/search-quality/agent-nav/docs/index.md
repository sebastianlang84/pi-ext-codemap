# Search quality fixture

This fixture documents the main implementation entrypoint, endpoint route handler, config-key lookup, error-message lookup, related tests, dependency declarations, and generated bundle noise.

ERR_NEWSLETTER_SNAPSHOT_STALE: macro snapshot is too old is documented here, but the throwing source is the expected navigation target.
